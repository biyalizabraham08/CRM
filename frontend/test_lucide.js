const lucide = require('lucide-react');
console.log(Object.keys(lucide).filter(k => k.toLowerCase().includes('message') || k.toLowerCase().includes('calendar') || k.toLowerCase().includes('list') || k.toLowerCase().includes('plus') || k.toLowerCase().includes('follow')));
